const intStateResults = {};
function reducerResults(state, action) {
  switch (action.type) {
    default:
      throw new Error("Invalid action");
  }
}

export { intStateResults };
export default reducerResults;
